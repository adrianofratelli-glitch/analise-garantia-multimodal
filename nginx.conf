worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
  worker_connections 1024;
}

http {
  include /etc/nginx/mime.types;
  default_type application/octet-stream;
  sendfile on;
  keepalive_timeout 65;

  server {
    listen 8080;
    server_name _;

    root /app/frontend/dist;
    index index.html;

    location /api/chamados/stream {
      proxy_pass http://127.0.0.1:8000;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_buffering off;          # SSE (Change Stream) precisa de flush imediato
      proxy_read_timeout 3600s;
    }

    location /api/ {
      proxy_pass http://127.0.0.1:8000;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_read_timeout 120s;  # chamadas de LLM podem demorar
    }

    location /media/ {
      proxy_pass http://127.0.0.1:8000;   # imagens servidas pelo FastAPI (storage local)
      proxy_http_version 1.1;
      proxy_set_header Host $host;
    }

    location /assets/ {
      try_files $uri =404;
      expires 7d;
      add_header Cache-Control "public, immutable";
    }

    location / {
      try_files $uri $uri/ /index.html;
    }
  }
}
